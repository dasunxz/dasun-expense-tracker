// Optional configuration for a future Supabase connection.
// The current app works fully with browser storage so GitHub Pages needs no backend.
window.EXPENSE_TRACKER_CONFIG = {
  supabaseUrl: "",
  supabaseAnonKey: "",
  useSupabase: false
};
